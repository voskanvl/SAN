include ../../common/panel/panel.pug
.mozaic
    each i in mozaic 
        - const color = i.color || "#fff"
        +panel(i.type,i.title,i.img)( class= i.right ? "panel_right" : "", style=`--img:url(${i.img}); --bg:${color}`,  )